# --- Web framework -----------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7
email-validator>=2.0
python-multipart>=0.0.9
python-dotenv>=1.0

# --- AI provider (Google Gemini — has a free tier) ----------------------
google-genai>=0.3

# --- Database -------------------------------------------------------------
sqlalchemy>=2.0
psycopg[binary]>=3.1

# --- Cache ------------------------------------------------------------------
redis>=5.0

# --- Background jobs -----------------------------------------------------------
celery>=5.4

# --- Auth -----------------------------------------------------------------------
PyJWT>=2.8
passlib[bcrypt]==1.7.4
bcrypt==4.3.0

# --- Resume parsing ----------------------------------------------------------------
pypdf>=4.0
python-docx>=1.1

# --- Dev / tests ----------------------------------------------------------------------
httpx>=0.27
pytest>=8.0
